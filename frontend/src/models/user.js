export default class User {
    constructor(role, username, email, password) {
        this.role = role;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}